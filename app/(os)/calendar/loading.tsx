import { CalendarSkeleton } from '@/components/os/skeletons'

/** Calendar: toolbar + month grid, the same shape the FullCalendar chunk loads behind. */
export default function CalendarLoading() {
  return <CalendarSkeleton />
}
