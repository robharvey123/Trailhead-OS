export default function SettingsPlaceholderPage() {
  return (
    <div className="rounded-[2rem] border border-slate-800 bg-slate-900/70 p-8">
      <p className="text-xs uppercase tracking-[0.32em] text-slate-500">Phase 1 placeholder</p>
      <h1 className="mt-3 text-3xl font-semibold text-slate-50">Settings</h1>
      <p className="mt-3 max-w-2xl text-sm text-slate-400">
        Settings is linked into the OS shell now so navigation is in place. The fuller settings surface can be layered in when you&apos;re ready to define what belongs here.
      </p>
    </div>
  )
}
